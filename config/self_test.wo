# Available commands:
# clear database
# learn context				(root)
# process changes			(p4|git, root, branch, start date, end date)
# extract keywords			(maximal count)
# extract global keywords	(maximal count)
# extract ownership         (team|author, threshold between 1 and 100)

task clear database

task learn context
	parser

task extract global keywords
	1000
